version: "3.7"

services:
  api:
    build:
      context: .
      dockerfile: src/FilterLists.Api/Dockerfile
      target: test-services
    networks:
      - test-services

networks:
  test-services:

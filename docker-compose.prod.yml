# overrides for production deployment

version: "3.8"

services:
  reverse_proxy:
    image: nginx:alpine
    ports:
      - "443:443"
    volumes:
      - ./reverse-proxy/conf.d:/etc/nginx/conf.d:ro
      - ~/cert:/etc/nginx/cert:ro
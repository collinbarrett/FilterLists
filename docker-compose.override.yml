version: '3.4'

services:
  filterlists.api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"

  filterlists.web:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"

